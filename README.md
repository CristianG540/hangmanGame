# Getting Started

### INSTALLING GLOBAL DEPENDENCIES

```sh
$ npm install -g sails
$ npm install -g bower
```

### INSTALLING PROJECT DEPENDENCIES
In the root of the project run:
```sh
$ npm install
$ bower install

### Lift Off!
In the root of the project run:
```sh
$ sails lift
```